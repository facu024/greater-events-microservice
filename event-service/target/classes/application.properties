server.port=8082
spring.application.name=event-service

# === DB local ===
spring.datasource.url=jdbc:postgresql://localhost:5432/eventdb
spring.datasource.username=facu
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
# opcional expl�cito:
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# === Eureka ===
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
spring.cloud.gateway.discovery.locator.enabled=false

management.endpoints.web.exposure.include=health,info,mappings

# === JWT (si el servicio usa tu JwtTokenUtil/JwtFilter) ===
jwt.secret='aaa'
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
jwt.issuer=${JWT_ISSUER:event-service}

# === (Opcional) llamadas a otros servicios por nombre l�gico ===
# Si este servicio llama al artist-service v�a Eureka/LoadBalancer/Feign,
# us� el nombre l�gico en las URLs (no localhost:puerto):
artist.service.url=http://artist-service

